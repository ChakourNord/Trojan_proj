using System;
using System.Collections.Generic;
using System.Text;
using System.Net;
using System.Net.Sockets;
using System.Threading;
using System.Linq;
using System.Diagnostics;
using Client;

namespace Threads
{
    class threads
    {
        public void thread1(Object list)
        {
            /*
            client fclient = new client();
            //Thread ts2 = new Thread(new ParameterizedThreadStart(startclient));
            //ts2.Start(list);
            
            Thread ts1 = new Thread(new ParameterizedThreadStart(fclient.startclient));
            ts1.Start(list);
            Thread ts2 = new Thread(fclient.empfang);
            ts2.Start();*/

        }
    }
}
